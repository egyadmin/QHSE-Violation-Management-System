# دليل تثبيت وتشغيل تطبيق QHSE
## خطوة بخطوة 🚀

---

## ⚠️ المشكلة الحالية:
Flutter SDK غير مثبت على النظام.

---

## 📥 الحل: تثبيت Flutter

### الطريقة 1: التثبيت اليدوي (موصى به)

#### الخطوة 1: تحميل Flutter SDK
1. افتح المتصفح واذهب إلى:
   ```
   https://docs.flutter.dev/get-started/install/windows
   ```

2. اضغط على زر التحميل للحصول على Flutter SDK
   - أو استخدم هذا الرابط المباشر:
   ```
   https://storage.googleapis.com/flutter_infra_release/releases/stable/windows/flutter_windows_3.16.0-stable.zip
   ```

#### الخطوة 2: فك الضغط
1. فك ضغط الملف الذي حملته
2. انقله إلى مكان ثابت، مثل:
   ```
   C:\src\flutter
   ```
   **مهم**: لا تضعه في `Program Files` لأنه يحتاج صلاحيات

#### الخطوة 3: إضافة Flutter للـ PATH
1. افتح "System Properties":
   - اضغط `Windows + R`
   - اكتب: `sysdm.cpl`
   - اضغط Enter

2. اذهب لتبويب "Advanced" → "Environment Variables"

3. في قسم "User variables"، اختر "Path" ثم "Edit"

4. اضغط "New" وأضف:
   ```
   C:\src\flutter\bin
   ```

5. اضغط OK على جميع النوافذ

#### الخطوة 4: إعادة تشغيل Terminal/VS Code
- أغلق VS Code تماماً
- افتحه من جديد

#### الخطوة 5: التحقق من التثبيت
افتح Terminal واكتب:
```bash
flutter doctor
```

سيظهر لك قائمة بالمتطلبات. لا تقلق من ✗، المهم أن Flutter يعمل!

---

### الطريقة 2: باستخدام Android Studio (الأسهل للمبتدئين)

#### الخطوة 1: تحميل Android Studio
```
https://developer.android.com/studio
```

#### الخطوة 2: تثبيت Flutter Plugin
1. افتح Android Studio
2. File → Settings (أو Ctrl+Alt+S)
3. Plugins → Marketplace
4. ابحث عن "Flutter"
5. اضغط Install
6. أعد تشغيل Android Studio

سيقوم بتثبيت Flutter SDK تلقائياً!

---

## 🚀 تشغيل التطبيق بعد تثبيت Flutter

### من VS Code:
1. افتح Terminal في VS Code (Ctrl + `)
2. تأكد أنك في مجلد المشروع:
   ```bash
   cd c:\QHSE\qhse_app
   ```

3. تثبيت المكتبات:
   ```bash
   flutter pub get
   ```

4. تشغيل التطبيق:
   ```bash
   # للتشغيل على Chrome (الأسهل)
   flutter run -d chrome
   
   # أو للتشغيل على Windows Desktop
   flutter run -d windows
   
   # أو اختيار الجهاز تلقائياً
   flutter run
   ```

### من Android Studio:
1. File → Open → اختر مجلد `c:\QHSE\qhse_app`
2. انتظر تحميل المشروع
3. اختر جهاز/محاكي من القائمة العلوية
4. اضغط Run (▶️)

---

## 🎯 اختصارات سريعة

### التشغيل السريع على Chrome (بدون محاكي):
```bash
cd c:\QHSE\qhse_app
flutter pub get
flutter run -d chrome
```

### التشغيل على Windows Desktop:
```bash
flutter config --enable-windows-desktop
flutter run -d windows
```

---

## ✅ التحقق من نجاح التثبيت

بعد تثبيت Flutter، جرب:
```bash
flutter --version
flutter doctor
```

يجب أن تظهر معلومات Flutter!

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "flutter: command not found"
**الحل**: أعد تشغيل الكمبيوتر بعد إضافة PATH

### مشكلة: "Unable to locate Android SDK"
**الحل**: لا تقلق! يمكنك التشغيل على Chrome أو Windows بدون Android SDK

### مشكلة: "cmdline-tools component is missing"
**الحل**: يمكنك تجاهل هذا إذا كنت ستشغل على Chrome فقط

---

## 📱 بعد التشغيل بنجاح

ستجد التطبيق يعمل مع:
- ✅ شاشة تسجيل دخول جميلة
- ✅ لوحة تحكم شاملة
- ✅ Bottom Navigation مع زر في المنتصف
- ✅ دعم العربية والإنجليزية
- ✅ اتصال بـ API موجود

---

## 🆘 تحتاج مساعدة؟

إذا واجهت أي مشكلة:
1. أرسل لي Screenshot للخطأ
2. أو أخبرني في أي خطوة توقفت
3. وسأساعدك مباشرة!

---

## 🎉 كل شيء جاهز!

التطبيق موجود في:
```
c:\QHSE\qhse_app
```

فقط تحتاج تثبيت Flutter وستعمل مباشرة! 🚀

**آخر تحديث**: 8 ديسمبر 2025
